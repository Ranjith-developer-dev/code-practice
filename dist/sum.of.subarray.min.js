"use strict";
function sumOfMins(arr) {
    const MOD = 1e9 + 7;
    const n = arr.length;
    let result = 0;
    const stack = [];
    for (let i = 0; i <= n; i++) {
        while (stack.length > 0 && (i === n || arr[i] < arr[stack[stack.length - 1]])) {
            const j = stack.pop();
            const k = stack.length > 0 ? stack[stack.length - 1] : -1;
            result = (result + arr[j] * (i - j) * (j - k)) % MOD;
        }
        stack.push(i);
    }
    return result;
}
const arr = [3, 1, 2, 4];
const minSUbArray = sumOfMins(arr);
console.log("🚀 ~ minSUbArray:", minSUbArray);
//# sourceMappingURL=sum.of.subarray.min.js.map